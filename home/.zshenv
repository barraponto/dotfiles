[[ -s "$HOME/.local/opt/nvm/nvm.sh" ]] && source "$HOME/.local/opt/nvm/nvm.sh"  # This loads NVM
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # This loads RVM
[[ -s "$HOME/.tmuxinator/scripts/tmuxinator" ]] && source "$HOME/.tmuxinator/scripts/tmuxinator" # This loads tmuxinator
